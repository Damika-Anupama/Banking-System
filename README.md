# Banking System - Full Stack Source

This branch preserves the complete application source for engineering review.

- `frontend/` - Angular client copied from the original frontend `optimize-dashboard-components` branch.
- `backend/` - Node.js/Express + MySQL API copied from the original backend `main` branch.

Use the `main` branch for the client-ready frontend demo deployment.
